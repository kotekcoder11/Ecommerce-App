name: flutter_application_1 # change if your package name is different
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  http: ^1.2.2
  cached_network_image: ^3.4.1
  flutter_spinkit: ^5.2.1
  intl: ^0.19.0
  google_fonts: ^6.2.1 # required

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
